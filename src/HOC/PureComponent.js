import React from 'react'

const PureComponent = () => {
  return (
    <div className='pureComponent'>
        <h1>Pure Component</h1>
        <div>

      A React component is said to be a pure component if it renders the same output for the same States and props.<br/>
      every component in react is pure component .
        </div>
    </div>
  )
}

export default PureComponent
